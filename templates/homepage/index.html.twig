{% extends 'base.html.twig' %}

{% block title %}Hello HomepageController!{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="text-center mb-4">
      <p class="lead">Please register or log in to continue.</p>
    </div>

    <div class="d-flex justify-content-center gap-3 btn-danger">
        <a href="{{ path('app_register') }}" class="btn btn-primary btn-lg">
            Register as User
        </a>
        <a href="{{ path('app_login') }}" class="btn btn-outline-secondary btn-lg">
            Login
        </a>
    </div>
</div>

{% endblock %}
